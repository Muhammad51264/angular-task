import { AppShadowEffectDirective } from './app-shadow-effect.directive';

describe('AppShadowEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new AppShadowEffectDirective();
    expect(directive).toBeTruthy();
  });
});
